import TimelineClient from "./TimelineClient";

export const metadata = {
  title: "KeenKeeper | Timeline",
  description: "View friendship history.",
};


const TimeLinePage = () => {
  
  return (
    <TimelineClient></TimelineClient>    
  );
};

export default TimeLinePage;
